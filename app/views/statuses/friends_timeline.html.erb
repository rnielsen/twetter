<% @tweets.each do |tweet| %>
<div class="tweet <%=cycle("on", "off") %>">
	<h2><%= h tweet.user.username %></h2>
	<p><%= h tweet.tweet %></p>
	<span class="datetime">[relative timestamp?]</p>
</div>
<% end %>
