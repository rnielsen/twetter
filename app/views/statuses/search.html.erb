<% setup(:title=>"Search", :body_id=>"search", :body_classes=>"status replyable", :css=>['timeline']) %>
<%= render :partial => "statuses/search" %>

<%if !@keyword.nil? && @keyword.length > 0 %>
    <h2 class="timeline-header">Search Results</h2>
    <%if @tweets.length > 0 %>

        <h3 class="timeline-subheader">Updates containing: '<%= @keyword %>'</h3>
        <%if @max_results %>
            <h3 class="timeline-subheader"><em>Note: Results capped at <%=@max_results %> updates.</em></h3>
        <% end %>

        <%= render :partial => "statuses/tweets" %>
    <% else %>
        <h3 class="timeline-subheader">No updates found containing '<%= @keyword %>'</h3>
    <% end %>
<% end %>
<%= render :partial => "statuses/sidebar" %>
